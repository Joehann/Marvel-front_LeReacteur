import "./search-input.scss";
const SearchInput = () => {
  return (
    <input
      className="search-input"
      type="text"
      placeholder="Votre recherche..."
    />
  );
};

export default SearchInput;
